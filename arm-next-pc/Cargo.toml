[package]
name = "arm-next-pc"
version = "0.1.0"
edition = "2024"

[features]
std = []

[dependencies]
asm-rs-macros = { version = "0.2.0", default-features = false }
yaxpeax-arch = { version = "0.3.2", default-features = false }
yaxpeax-arm = { version = "0.5.0", default-features = false }
